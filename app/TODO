Upload files only if they are encrypted (DONE)
Add/Edit notes only if they are unencrypted (DONE)
Delete notes even if they are encrypted or decrypted. (DONE)
User should select number(weak encryption but easy to remember) or password for encryption
(strong for encryption but harder to remember). Show what happens if you forget the password
(NOT DONE, NOT GOING TO DO IT)

After decryption if the data looks ok (stored in preference manager) updateUI
After decryption if the data does not look ok (stored in preference manager) show help button
Show to reverse the decryption with the same password if the data does not look ok. And try
another decryption password

Large note test (Encrypt on another thread, EditNote on another thread, Load adapter notes
on another thread)



