<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="instant.moveadapt.com.backedupnotes">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".NotesListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".NewNoteActivity"
            android:parentActivityName=".NotesListActivity" />

        <activity android:name=".EditNoteActivity"
            android:parentActivityName=".NotesListActivity"/>

        <activity android:name=".Crypt"
            android:parentActivityName=".NotesListActivity"/>

        <activity android:name=".LoginActivity"
            android:parentActivityName=".NotesListActivity"/>

        <provider
            android:authorities="instant.moveadapt.com.backedupnotes.notes;instant.moveadapt.com.backedupnotes.toBeDeleted"
            android:name=".Database.NotesContentProvider"/>
    </application>

</manifest>